// @flow

import { StyleSheet } from 'react-native';

import { MARKERS_SIZES } from '../../../constants';

export default StyleSheet.create({
  root: {
    height: MARKERS_SIZES.CONTAINER_HEIGHT,
  },
});
