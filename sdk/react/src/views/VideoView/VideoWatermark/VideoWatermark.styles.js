// @flow

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  watermarkContainer: {
    flex: 0,
    flexDirection: 'row',
    justifyContent: 'flex-end',
    padding: 10,
  },
});
