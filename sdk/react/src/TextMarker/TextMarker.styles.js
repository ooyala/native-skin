// @flow

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  root: {
    backgroundColor: 'red',
    bottom: 10,
    color: 'white',
    left: 0,
    position: 'absolute',
    width: 100,
  },
});
