// @flow

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  root: {
    backgroundColor: 'red',
    bottom: 10,
    color: 'white',
    position: 'absolute',
    width: 100,
  },
});
